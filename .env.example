# sundev has no environment variables for local dev or the Astro build.
# Deployment uses GitHub Actions secrets (KYLOS_*), not a checked-in .env file.
#
# If you add build-time or runtime config later, document names here — never real values.
